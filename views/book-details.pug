//- book-details.pug :
//- book detail page.
extends layout.pug
block content
	h1 Book Details:

	p
		label(for='title') Title:
		input#author(name='title', type='text', value=`${book.title}`)
	p
		label(for='author') Author:
		input#author(name='author', type='text', value=`${book.author}`)
	p
		label(for='genre') Genre:
		input#genre(name='genre', type='text', value=`${book.genre}`)
	p
		label(for='year') Year:
		input#year(name='year', type='text', value=`${book.year}`)
	h3 Options:
	p
		input(type='submit', value='Update Book')
		form(method='post', action=`/books/${book.id}/delete`, onsubmit=`return confirm('Do you really want to delete ${book.title}?');`)
	p
		input(type='submit', value='Delete Book')

	a(href='/') ← Back to Home


