//- error.pug :  
//- for displaying a user friendly error message.
extends layout.pug

block content
	h1= error.message
  	h2= error.status
  	pre #{error.stack}
	a(href='/') ← Back to Home
